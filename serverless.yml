service: kakao-payment-test

provider:
  name: aws
  runtime: nodejs10.x

plugins:
  - serverless-offline

region: ap-northeast-2

functions:
  payment:
    handler: handler.payment
    events:
      - http:
          path: /
          method: get
